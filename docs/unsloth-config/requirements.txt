# Getestet auf CUDA 12.1+ / Python 3.11 / RTX 4000 Ada (Ada Lovelace).
# Pins sind bewusst gesetzt — Unsloth/TRL/transformers/mistral-common müssen
# als Tupel zusammenpassen, sonst bricht entweder load_in_4bit, das Chat-
# Template oder train_on_responses_only.
#
# Ziel-Modell: unsloth/Mistral-Small-3.2-24B-Instruct-2506-unsloth-bnb-4bit
# (Juni-2025-Release, Tekken-V7-Tokenizer, Pixtral-Vision-Encoder).

# Kernkomponenten — Mistral-Small-3.2 braucht transformers >= 4.51
# (Mistral3ForConditionalGeneration + Pixtral-Vision-Tower), TRL >= 0.15
# für die SFTTrainer-API mit dem neuen Tokenizer.
torch==2.5.1
transformers==4.51.3
trl==0.15.2
peft==0.14.0
accelerate==1.4.0

# Unsloth — Pin auf Dez-2025-Release, der Mistral-Small-3.2 supportet.
# Falls Studio-UI statt CLI: `pip install unsloth` reicht dort separat.
unsloth @ git+https://github.com/unslothai/unsloth.git@2025.12.4

# 4-bit Quantisierung
bitsandbytes==0.45.0

# Mistral-Common Tokenizer (Tekken-V7 für Mistral-Small-3.2)
mistral-common>=1.6.0

# Tokenizer / Sentencepiece (Mistral braucht weiterhin protobuf)
sentencepiece==0.2.0
protobuf==4.25.5

# Dataset-Handling
datasets==3.2.0

# GGUF-Export (llama.cpp-Backend, wird beim ersten Aufruf automatisch gebaut)
# Falls du den Build vermeiden willst: `save_pretrained_gguf` auskommentieren
# und GGUF manuell via `llama.cpp`'s `convert_hf_to_gguf.py` erzeugen.

# Monitoring (optional)
tensorboard==2.18.0
