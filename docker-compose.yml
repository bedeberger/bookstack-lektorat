services:
  lektorat:
    build: .
    ports:
      - "3737:3737"
    volumes:
      - lektorat_data:/app/data
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/lektorat.db
    restart: unless-stopped

volumes:
  lektorat_data:
